apiVersion: 1

providers:
  - name: "vswrite"
    orgId: 1
    folder: "VsWrite"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards
